name: CI

on:
  push:
    branches: [main, '**']
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libegl1 libgl1 libxkbcommon0 libxcb1 libx11-6 \
            libwayland-client0 libxfixes3

      - name: Setup Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: clippy

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: cargo check
        run: cargo check

      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings

  build-linux:
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libegl1 libgl1 libxkbcommon0 libxcb1 libx11-6 \
            libwayland-client0 libxrandr2 libxi6 libxxf86vm1 libxfixes3 libglu1-mesa

      - name: Build Linux release
        run: cargo build --release

      - name: Upload Linux binary
        uses: actions/upload-artifact@v4
        with:
          name: calce-linux
          path: target/release/calce

  build-windows:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install MinGW-w64 cross toolchain
        run: sudo apt-get install -y -qq gcc-mingw-w64-x86-64

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-windows-${{ hashFiles('**/Cargo.lock') }}

      - name: Find MinGW GCC lib dir
        id: gcclib
        run: echo "path=$(ls -d /usr/lib/gcc/x86_64-w64-mingw32/*/ | head -n1)" >> $GITHUB_OUTPUT

      - name: Build Windows release
        env:
          CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: x86_64-w64-mingw32-gcc
          MINGW_SYSROOT_LIB: /usr/x86_64-w64-mingw32/lib
          MINGW_GCC_LIB: ${{ steps.gcclib.outputs.path }}
        run: cargo build --release --target x86_64-pc-windows-gnu

      - name: Upload Windows binary
        uses: actions/upload-artifact@v4
        with:
          name: calce-windows
          path: target/x86_64-pc-windows-gnu/release/calce.exe

  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK cmdline-tools
        run: |
          if [ ! -d "$ANDROID_HOME/cmdline-tools/latest" ]; then
            curl -fSL -o /tmp/cmdlinetools.zip "https://dl.google.com/android/repository/commandlinetools-linux-latest.zip"
            mkdir -p "$ANDROID_HOME/cmdline-tools/latest"
            unzip -q /tmp/cmdlinetools.zip -d "$ANDROID_HOME/cmdline-tools/latest"
            rm /tmp/cmdlinetools.zip
          fi
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}

      - name: Install NDK via sdkmanager
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            --sdk_root="$ANDROID_HOME" \
            "platform-tools" "build-tools;34.0.0" \
            "platforms;android-34" "ndk;25.1.8394850"

      - name: Install cargo-apk
        run: cargo install cargo-apk --locked

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-android-${{ hashFiles('**/Cargo.lock') }}

      - name: Build Android APK
        run: cargo apk build --release

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: calce-android
          path: |
            target/release/apk/calce.apk